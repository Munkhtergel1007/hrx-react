import {
} from "../actionTypes";
import config from "../config";
const initialState = {
};

export default(state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};